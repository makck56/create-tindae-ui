/**
 * {{featureChineseName}} API
 * @feature {{featureKebab}}
 */
import { http } from '@/core/http';
import type { Result, PaginationParams, PaginationResult } from '@/types/api';
import type { {{featurePascal}}, Create{{featurePascal}}Params } from '../models/{{featureCamel}}';

/**
 * 获取{{featureChineseName}}列表
 */
export const get{{featurePascal}}List = (params: PaginationParams & { keyword?: string }) => {
  return http.get<Result<PaginationResult<{{featurePascal}}>>>('/{{domainKebab}}/{{featureKebab}}/list', { params });
};

/**
 * 创建{{featureChineseName}}
 */
export const create{{featurePascal}} = (params: Create{{featurePascal}}Params) => {
  return http.post<Result<{{featurePascal}}>>('/{{domainKebab}}/{{featureKebab}}', params);
};

/**
 * 更新{{featureChineseName}}
 */
export const update{{featurePascal}} = (id: string, params: Partial<Create{{featurePascal}}Params>) => {
  return http.put<Result<{{featurePascal}}>>(`/{{domainKebab}}/{{featureKebab}}/${id}`, params);
};

/**
 * 删除{{featureChineseName}}
 */
export const delete{{featurePascal}} = (id: string) => {
  return http.delete<Result<void>>(`/{{domainKebab}}/{{featureKebab}}/${id}`);
};
