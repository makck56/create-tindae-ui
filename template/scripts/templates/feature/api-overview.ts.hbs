import { request } from '@/core/http';
import type { {{featurePascal}}Overview } from '../models/{{featurePascal}}';

/**
 * 获取 {{featureChineseName}} 概览数据（KPI 统计 + 近期列表）。
 *
 * 概览型为只读视图，仅需一个聚合查询接口，无 CRUD。
 * 返回 ApiResponse<{{featurePascal}}Overview>，调用方直接 res.data 取业务数据。
 */
export const get{{featurePascal}}Overview = () => {
  return request.get<{{featurePascal}}Overview>('/{{domainKebab}}/{{featureKebab}}/overview');
};
